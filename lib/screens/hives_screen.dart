import 'package:flutter/material.dart';

class HivesScreen extends StatelessWidget {
  const HivesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Tvoje včelíny budou tady 🍯',
        style: TextStyle(fontSize: 18),
      ),
    );
  }
}
